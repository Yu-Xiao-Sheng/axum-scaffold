# create-axum-app Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-02-06

## Active Technologies

- Rust 2024 Edition (latest stable) + clap (CLI parsing), inquire (interactive prompts), handlebars (template rendering), axum (web framework), tokio (async runtime), tracing (structured logging), sqlx (database), serde (serialization), biz-error (error handling), jsonwebtoken (JWT), thiserror (error types), anyhow (error context) (001-cli-mvp)

## Project Structure

```text
src/
tests/
```

## Commands

cargo test [ONLY COMMANDS FOR ACTIVE TECHNOLOGIES][ONLY COMMANDS FOR ACTIVE TECHNOLOGIES] cargo clippy

## Code Style

Rust 2024 Edition (latest stable): Follow standard conventions

## Recent Changes

- 001-cli-mvp: Added Rust 2024 Edition (latest stable) + clap (CLI parsing), inquire (interactive prompts), handlebars (template rendering), axum (web framework), tokio (async runtime), tracing (structured logging), sqlx (database), serde (serialization), biz-error (error handling), jsonwebtoken (JWT), thiserror (error types), anyhow (error context)

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
